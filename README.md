# Configuring Nginx server to run over HTTPS
## Install ansible galaxy roles
```bash
make install
```
## Build the infrastructure and provision the server
```bash
make build
```
## Run all in one command
```bash
make start
```
## Clean
```bash
make destroy
```